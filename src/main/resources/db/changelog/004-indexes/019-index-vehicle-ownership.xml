<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">
    <changeSet id="019-index-vehicle-ownership" author="nour">
        <sql splitStatements="true" stripComments="true">
            CREATE INDEX IF NOT EXISTS idx_vehicle_ownership_vehicle_id
                ON vehicle_ownership (vehicle_id);
            CREATE INDEX IF NOT EXISTS idx_vehicle_ownership_owner_id
                ON vehicle_ownership (owner_id);
            CREATE INDEX IF NOT EXISTS idx_vehicle_ownership_is_current
                ON vehicle_ownership (vehicle_id, is_current)
                WHERE is_current = TRUE;
        </sql>
    </changeSet>
</databaseChangeLog>

