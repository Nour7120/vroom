<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">
    <changeSet id="005-create-vehicle-mileage-history" author="nour">
        <sql splitStatements="true" stripComments="true">
            CREATE TABLE IF NOT EXISTS vehicle_mileage_history (
                id              BIGINT          GENERATED BY DEFAULT AS IDENTITY    PRIMARY KEY,
                vehicle_id      BIGINT          NOT NULL,
                mileage         BIGINT          NOT NULL,
                recorded_date   DATE            NOT NULL,
                source          VARCHAR(50),
                created_at      TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
                created_by      VARCHAR(255)
            );

            COMMENT ON TABLE vehicle_mileage_history IS 'Mileage timeline snapshots used for mileage graph and audit';
        </sql>
    </changeSet>
</databaseChangeLog>

