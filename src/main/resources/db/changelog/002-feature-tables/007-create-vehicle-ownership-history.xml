<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">
    <changeSet id="007-create-vehicle-ownership-history" author="nour">
        <sql splitStatements="true" stripComments="true">
            CREATE TABLE IF NOT EXISTS vehicle_ownership_history (
                id               BIGINT          GENERATED BY DEFAULT AS IDENTITY    PRIMARY KEY,
                vehicle_id       BIGINT          NOT NULL,
                owner_id         BIGINT,
                ownership_start  DATE,
                ownership_end    DATE,
                created_at       TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
                created_by       VARCHAR(255)
            );

            COMMENT ON TABLE vehicle_ownership_history IS 'Ownership timeline used in the Vehicle Passport. NULL ownership_end = current owner';
        </sql>
    </changeSet>
</databaseChangeLog>

